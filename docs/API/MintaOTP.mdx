import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

---
Minta OTP
---
Gunakan endpoint ini untuk melakukan **Cek saldo pengguna**.

---

## 🧭 Endpoint
| Atribut | Nilai |
|----------|-------|
| **URL** | `https://api.hesda-store.com/v2/saldo?hesdastore={key}` |
| **Method** | `GET` |
| **Auth** | Basic Auth (`Authorization: email & password`) |
| **Content-Type** | `application/json` |

---

## 📥 Parameter

| Nama | Tipe | Wajib | Keterangan |
|------|------|--------|-------------|
| `hesdastore` | `string` | ✅ | `API Key` |
| `no_hp` | `number` | ✅ | ID unik pengguna |
| `metode` | `string` | ✅ | Jenis metode (`OTP`) |

---

## 📘 Contoh Request

```bash
curl -X POST https://api.hesdapay.com/v1/topup \
-H "Content-Type: application/json" \
-H "Authorization: Bearer 1234567890abcdef" \
-d '{
  "user_id": "USR001",
  "nominal": 50000,
  "metode": "qris"
},
```
---

:::info 🧪 Contoh Response JSON

Beberapa contoh response berdasarkan status transaksi:

<Tabs>
  <TabItem value="success" label="🟢 Success">
    ```json
    {
        "status": true,
        "message": "Data ditemukan!",
        "data": {
            "saldo": 6406323
        }
    }
    ```

  </TabItem>

  <TabItem value="pending" label="🟡 Pending">
    ```json
    {
        "status": true,
        "message": "Data ditemukan!",
        "data": {
            "saldo": 0
        }
    }
    ```
  </TabItem>

  <TabItem value="failed" label="🔴 Failed">
    ```json
    {
        "status": false,
        "message": "Unauthorized"
    }
    ```
  </TabItem>

  <TabItem value="saldo" label="🔴 Saldo Habis">
    ```json
    {
        "status": true,
        "message": "Data ditemukan!",
        "data": {
            "saldo": 0
        }
    }
    ```
  </TabItem>

</Tabs>

:::