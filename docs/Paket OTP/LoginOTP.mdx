import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

---
Login OTP
---
Gunakan endpoint ini untuk melakukan **Login OTP**.

---

## 🧭 Endpoint
| Atribut | Nilai |
|----------|-------|
| **URL** | `https://api.hesda-store.com/v2/login_sms` |
| **Method** | `POST` |
| **Auth** | Basic Auth (`Authorization: email & password`) |
| **Content-Type** | `application/json` |

---

## 📥 Body

| Nama | Tipe | Wajib | Keterangan |
|------|------|--------|-------------|
| `hesdastore` | `string` | ✅ | `API Key` |
| `no_hp` | `number` | ✅ | Nomor XL |
| `metode` | `string` | ✅ | Jenis metode (`OTP`) |
| `auth_id` | `string` | ✅ | `didapat dari minta OTP` |
| `kode_otp` | `string` | ✅ | `Kode OTP 6 Digit` |

---

:::info 🧪 Response JSON

Beberapa contoh response berdasarkan status transaksi:

<Tabs>
  <TabItem value="success" label="🟢 Success">
    ```json
    {
        "statusCode": 200,
        "status": true,
        "message": "Berhasil mengirim kode OTP, silakan cek SMS di nomor XL kamu!",
        "data": {
            "auth_id": "28b028f7-69bd-417e-8510-691d23df7d25",
            "can_resend_in": 60
        }
    }
    ```
  </TabItem>

  <TabItem value="failed" label="🔴 Failed">
    ```json
    {
        "statusCode": 200,
        "status": false,
        "message": "Terjadi kesalahan, pastikan nomor XL/AXIS/LIVEON yang kamu masukkan benar (terdaftar NIK & KK) dan mohon coba lagi! Error Message: User not found | Error Description: -",
        "data": []
    }
    ```
  </TabItem>

</Tabs>

:::

---
